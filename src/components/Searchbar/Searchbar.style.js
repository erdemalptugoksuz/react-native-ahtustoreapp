import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  searchBox: {
    backgroundColor: 'aqua',
    margin: 10,
    padding: 15,
    borderRadius: 10,
    fontSize: 18,
  },
});
